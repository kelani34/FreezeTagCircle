# Decision Records

Use this folder for larger decision records when an entry in `docs/DECISION_LOG.md` needs more detail.

Keep the main decision log as the index. Add separate files here only for decisions with meaningful context, alternatives, or follow-up analysis.

